<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--item name="colorPrimary">@color/colorPrimary</item-->
        <!--item name="colorPrimaryDark">@color/colorPrimaryDark</item-->
        <!--item name="colorAccent">@color/colorAccent</item-->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="team" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/meet_team</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/backgroud_splash</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/gradient</item>
        <!-- Support library compatibility -->
        <item name="background">@drawable/gradient</item>
    </style>

</resources>
